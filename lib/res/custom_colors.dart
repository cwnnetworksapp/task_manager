import 'package:flutter/material.dart';

/// A class for defining custom color constants used in the app.
class CustomColors {
  /// Color representing Firebase's navy blue.
  static final Color firebaseNavy = Color(0xFF2C384A);

  /// Color representing Firebase's orange.
  static final Color firebaseOrange = Color(0xFFF57C00);

  /// Color representing Firebase's amber.
  static final Color firebaseAmber = Color(0xFFFFA000);

  /// Color representing Firebase's yellow.
  static final Color firebaseYellow = Color(0xFFFFCA28);

  /// Color representing Firebase's grey.
  static final Color firebaseGrey = Color(0xFFECEFF1);

  /// Color representing Google's background blue.
  static final Color googleBackground = Color(0xFF4285F4);
}
